{
  "name": "confidential-prenup",
  "version": "1.0.0",
  "description": "Confidential Prenuptial Agreement on Midnight Blockchain",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist logs build",
    "build": "npm run compile && tsc",
    "compile": "midnight compile src/managed/agreement.compact -o build",
    "deploy": "node --loader ts-node/esm src/deploy.ts",
    "deploy:simple": "node src/deploy.js",
    "test": "echo \"No tests specified\" && exit 0",
    "verify": "node src/verify.ts"
  },
  "keywords": [
    "midnight",
    "blockchain",
    "prenup",
    "confidential",
    "smart-contract"
  ],
  "author": "Your Name",
  "license": "Apache-2.0",
  "dependencies": {
    "@midnight/midnight-js": "^0.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "rimraf": "^5.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}